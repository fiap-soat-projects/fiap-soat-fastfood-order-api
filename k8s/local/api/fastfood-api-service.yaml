apiVersion: v1
kind: Service
metadata:
  name: fastfood-order-api
  namespace: fiap
  labels:
    app: fastfood-order-api
spec:
  selector:
    app: fastfood-order-api  
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort